---
import Button from "./ButtonCTA.astro";
---

<section class="hero">
  <div class="hero__text">
    <h1 class="hero__text-title">
      Descubra o seu
      <strong>potencial,</strong> encontre
      <strong>equilíbrio</strong> e seja
      <strong>você</strong> mesmo
    </h1>
    <p class="hero__text-paragraph">
      Olá, me chamo Samantha, sou psicóloga e acredito que o autoconhecimento é
      o primeiro passo para se ter o tão desejado equilíbrio emocional
    </p>
    <Button>
      <img
        class="hero__button-img"
        src="/icons/edit.svg"
        alt="Agende sua consulta"
      />
      Agende sua Consulta
    </Button>
  </div>
  <div>
    <img class="hero__img" src="/heroimg.png" alt="Samantha Dutra" />
  </div>
  <span class="hero-arrow-down">
    <img loading="lazy" src="/icons/arrow-down.svg" alt="Arrow Down" />
  </span>
</section>

<style lang="scss">
  .hero {
    min-height: calc(100vh - 92px);
    max-width: 75rem; //1200px
    width: 100%;

    padding: 1rem;
    margin: 0 auto;

    display: flex;
    align-items: center;
    justify-content: space-around;
    flex-direction: column;
    gap: 2.25rem;

    position: relative;

    &__text {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 2rem;

      &-title {
        font-size: clamp(3rem, 6vw, 3.5rem);
        font-weight: bold;
        color: var(--color-blue);
        line-height: 1.3;

        strong {
          color: var(--accent-orange);
        }
      }

      &-paragraph {
        max-width: 70ch;
        font-size: 1.125rem;
        color: var(--color-dark);
        padding-bottom: 1.5rem;
        line-height: 1.3;
      }
    }

    &__button-img {
      width: 20px;
      margin-right: 0.5rem;
    }

    &__img {
      padding-bottom: 5rem;
    }

    .hero-arrow-down {
      position: absolute;
      bottom: 2.5rem;
      animation: upAndDown 3s ease infinite;
    }
  }

  @media screen and (min-width: 800px) {
    .hero {
      flex-direction: row;

      &__title {
        max-width: 22ch;
      }

      &__text {
        &-paragraph {
          line-height: 2;
        }
      }

      &__img {
        margin-right: 2.6vw;
        padding-bottom: 0;
      }
    }
  }

  @keyframes upAndDown {
    0% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
    100% {
      transform: translateY(0);
    }
  }
</style>

<script></script>

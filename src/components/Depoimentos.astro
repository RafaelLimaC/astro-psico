---
import "swiper/css";
import Detail from "./Detail.astro";
---

<section class="depoimentos" id="depoimentos">
  <div class="swiper swiper-container">
    <div class="swiper-wrapper">
      <div class="swiper-slide">
        <div class="swiper-content">
          <img src="/aspas-swiper.svg" alt="Aspas" />
          <p>
            É uma profissional atenciosa e está fazendo um belo trabalho com a
            minha filha neste ano. Dá pra perceber os resultados.
          </p>
          <div class="swiper__slide-cliente">
            <span>MARCOS R.</span>
            <span>Cliente</span>
          </div>
        </div>
      </div>
      <div class="swiper-slide">
        <div class="swiper-content">
          <img src="/aspas-swiper.svg" alt="Aspas" />
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit.
            Pellentesque id nulla scelerisque, pretium enim non, fringilla
            sapien. Sed ac enim nec augue faucibus consequat.
          </p>
          <div class="swiper__slide-cliente">
            <span>PEDRO L.</span>
            <span>Cliente</span>
          </div>
        </div>
      </div>
    </div>
    <div class="swiper-button-next"></div>
    <div class="swiper-button-prev"></div>
  </div>
  <Detail />
</section>

<style lang="scss">
  .depoimentos {
    margin: 0 auto;
    background: var(--background-gradient);
    padding: 5rem 0 3.5rem;
    position: relative;
    box-shadow: -5px -5px 20px #f4752152;
  }

  .swiper-container {
    padding: 1rem 2.5rem;
    width: 100%;
    max-width: 76.25rem;
  }

  .swiper-slide {
    padding: 1rem;
    cursor: grab;

    .swiper-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 5rem;

      img {
        height: 40px;
      }

      p {
        font-size: 1.5rem;
        font-weight: 400;
        line-height: 1.25;
        color: var(--accent-blue);
        text-align: center;
        max-width: 45ch;
        font-family: var(--ff-roboto-slab);
      }

      .swiper__slide-cliente {
        display: flex;
        flex-direction: column;
        align-items: center;

        span {
          font-size: 1.3rem;
          line-height: 1.3;
          font-weight: 400;
          color: var(--accent-orange);
          letter-spacing: 0.1em;
        }

        span:last-child {
          font-size: 1.3rem;
          letter-spacing: 0;
          line-height: 1.5;
          font-weight: 400;
          color: var(--color-dark);
        }
      }
    }
  }

  @media screen and (min-width: 800px) {
    .depoimentos {
      padding: 6.5rem 0 4.5rem;
    }

    .swiper-container {
      .swiper-slide {
        .swiper-content {
          gap: 4rem;

          p {
            font-size: 2.25rem;
          }
        }
      }
    }
  }

  :root {
    --swiper-navigation-color: #fac3b9;
    --swiper-navigation-size: 40px;
  }
</style>

<script>
  import Swiper from "swiper";
  import { Navigation, Pagination } from "swiper/modules";

  Swiper.use([Navigation, Pagination]);
  const swiper = new Swiper(".swiper", {
    loop: true,
    slidesPerView: 1,
    spaceBetween: 50,
    navigation: {
      nextEl: ".swiper-button-next",
      prevEl: ".swiper-button-prev",
    },
  });
</script>

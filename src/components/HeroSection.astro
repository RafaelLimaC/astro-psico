---
import Button from "./ButtonCTA.astro";
import Detail from "./Detail.astro";
---

<section>
  <div class="hero">
    <div class="hero__text">
      <h1 class="hero__text-title">
        Avaliação <strong>Neuropsicológica</strong> e <strong
          >Psicoterapia</strong
        > Individual
      </h1>
      <p class="hero__text-paragraph">
        Invista em autoconhecimento profundo. Um caminho de descobertas, com
        foco em saúde, significado e qualidade de vida.
      </p>
      <Button class="orange">
        <img
          class="hero__button-img"
          src="/icons/edit.svg"
          alt="Agende sua consulta"
        />
        Agende sua Consulta
      </Button>
    </div>
    <div>
      <img class="hero__img" src="/heroimg.png" alt="Samantha Dutra" />
    </div>
    <span class="hero-arrow-down">
      <img loading="lazy" src="/icons/arrow-down.svg" alt="Arrow Down" />
    </span>
  </div>
  <Detail />
</section>

<style lang="scss">
  section {
    position: relative;
  }

  .hero {
    min-height: calc(100vh - 92px);
    max-width: 76.25rem; //1220px
    width: 100%;

    padding: 1rem;
    margin: 0 auto;

    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-direction: column;
    gap: 2.25rem;

    position: relative;

    &__text {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      margin-top: 2rem;

      &-title {
        font-size: clamp(2.25rem, 6vw, 3.5rem);
        font-weight: bold;
        color: var(--color-blue);
        line-height: 1.3;
        max-width: 22ch;

        strong {
          color: var(--accent-orange);
        }
      }

      &-paragraph {
        max-width: 70ch;
        font-size: 1.125rem;
        color: var(--color-dark);
        padding-bottom: 0.5rem;
        line-height: 1.3;
      }
    }

    &__button-img {
      width: 20px;
      margin-right: 0.5rem;
    }

    &__img {
      padding-bottom: 5rem;
    }

    .hero-arrow-down {
      position: absolute;
      bottom: 2.5rem;
      left: 50%;
      animation: upAndDown 3s ease infinite;
    }
  }

  @media screen and (min-width: 800px) {
    .hero {
      flex-direction: row;

      &__title {
        max-width: 22ch;
      }

      &__text {
        &-paragraph {
          line-height: 2;
          padding-bottom: 2.5rem;
        }
      }

      &__img {
        margin-right: 2.6vw;
        padding-bottom: 0;
      }
    }
  }

  @keyframes upAndDown {
    0% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
    100% {
      transform: translateY(0);
    }
  }
</style>

<script></script>
